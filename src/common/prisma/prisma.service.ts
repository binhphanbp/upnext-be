// Compatibility export for modules that have not yet migrated their import path.
// The only provider is the global PrismaService from src/prisma/prisma.module.ts.
export { PrismaService } from '../../prisma/prisma.service';
